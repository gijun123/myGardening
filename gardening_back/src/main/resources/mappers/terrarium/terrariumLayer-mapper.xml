<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ggirick.gardening_back.mappers.terrarium.TerrariumLayerMapper">

    <insert id="insertLayer" parameterType="com.ggirick.gardening_back.dto.terrarium.TerrariumLayerDTO">
        <selectKey order="BEFORE" keyProperty="id" resultType="int">
            select terrarium_layer_seq.nextval from dual
        </selectKey>
        insert into terrarium_layer values(#{id},#{terrariumId},#{layerType},#{oriName},#{sysName},#{url},
        #{x},#{y},#{width},#{height},#{rotation},#{zIndex},default)
    </insert>

    <select id="getLayers" resultType="com.ggirick.gardening_back.dto.terrarium.TerrariumLayerDTO">
        select * from terrarium_layer where terrarium_id = #{terrariumId} order by z_index asc
    </select>
</mapper>